/*
createrand() function creates just one random number between 100-999 and returns it.
createarray() this function calls createrand function, gets numbers and assign to the array.
check() function controls the polindromic number and prints it.
array must be created in main function, don't use global variables.
*/
//WRITE THE CODE BELOW.
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int createrand();
void createarray(int num);
void check(int num);

int main (void){
    srand(time(NULL));
    return 0;
}

int createrand(){
    
}
void createarray(int num){
    
}
void check(int num){
    
}